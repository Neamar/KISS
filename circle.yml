test:
  pre:
    - emulator -avd circleci-android22 -no-audio -no-window:
        background: true
        parallel: true
  override:
    - ./gradlew assembleDebug
    - circle-android wait-for-boot
    - cp -r kiss/build/outputs $CIRCLE_ARTIFACTS